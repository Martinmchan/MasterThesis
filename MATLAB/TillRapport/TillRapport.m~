
close all
clear all
[mic1, f] = audioread('syncedmic1.wav');
[mic2, f] = audioread('syncedmic2.wav');



plot(mic1)
hold on
plot(mic2)

s = 1:200000

tdoa = ou



%%
s1 = 1:200000;
s2 = 200001:350000;


[syncedmic2, dist, deltaT] = ourSync(mic1, mic2, s1, s2);

figure
plot(mic1)
hold on
plot(syncedmic2)
